# DimBlend Experience 需求清单

> 2026-09-19 核对结论：除行内注明的排除项外，代码层面已全部实现，剩余为待实机验证；本清单状态统一为“待实测”。规格冲突以 `docs/requirements-spec.md` 为准。
>
> 2026-03-22 增量核对：按实测反馈回写状态（未实现 / 未测试 / 更改 / 回退 / 新条目）；`create:diesel_generator` CLEAR。
>
> 2026-09-20 对齐核对：逐条对照 `docs/requirements-spec.md`（冲突以 spec 为准）与当前代码；已实现但标签滞后的改为“待实测”，被 spec/新方案替代的标“作废”并注明去向，唯一真缺实现的是 z16 隐藏数字。
>
> 2026-09-21 合并核对（以 spec 为主）：逐条比对本次粘贴增量 × 三文档 × 代码实现——粘贴增量无新需求，全部已在三文档覆盖且 spec 口径更新（折返僵持/回程分段强制0/组装器 EntityPlaceEvent/刹车 0.5）；z16 隐藏数字代码已实现（`ZProgressHud`），清单“未实现”属文档滞后，已订正为“待实测”。
>
> 2026-09-22 粘贴增量同步：照用户粘贴原样回写状态（未实现 / 未测试 / 新条目），与 spec/代码口径冲突处行内注明“与现口径冲突、待拍板”，不直接改 spec。
>
> 2026-09-22 拍板结果：折返回血按严格口径（只认128）实施；进度条按同日晚补充的锁存口径实施（归0%锁存、回128/向前跨段解除，z256-bar §3.2/§4 已同步）；轨道低端按 0.5、刹车按音阶 1、马达加 3 秒延迟实施；组装器维持取消放置方案、实机验证。
> 新条目拍板（2026-09-22）：一律仅 rotating 维度；水按松版（只看相邻两格）；传送门下界/末地在内全禁、折跃门留块禁传；村民大师=完整大师交易（流浪商人除外）；床拦使用不拦破坏、创造豁免；宽轨粒子纯客户端。
> 第二批口径（2026-09-22 全票开工）：见 spec v1.5 + 板块 G（G1–G4）/ E6；回家通道不管。
> 2026-09-23 组装器拍板更新：取消方案实测物品消失不返还（客户端预测扣减得不到服务端槽位纠正），改「放行放置+破坏自身返还」实施，spec F1 已同步。
> 2026-09-23 深夜批次：D4 用户回标"未实现"成立（客户端 active 永不同步的根因已修，判据改同步 speed）；新条目 D5 交流发电机 <16rpm FE 流失、D6 马达护目镜应力量/已使用能量改实际转速，均已实施待实测，spec v1.7。
> 实测反馈修订：z256 进度条回程锁存口径作废，改为回程瞬时归零（转头前进即恢复，无记忆），`ZProgressHud` 去锁存记忆，三文档已同步，待实测。

## 探索限制与魔改

### 探索限制 mod

- [ ] 待实测｜死后需要清空经验条 不能找回
- [ ] 待实测｜玩家死后绝对不能在原点重生 如果床遗失 则在死亡位置x相同 y66 z5位置附近重生
- [ ] 待实测｜玩家z值绝对值每跨越256时 生命值在当前上限-25% 并播放angry_villager粒子效果
- [ ] 待实测｜玩家回到z128以内后完全恢复生命上限 并播放 happy_villager 粒子效果（原“未实现”，开发侧已实现，待实测）
- [ ] 待实测｜玩家折返往回走时 除非回到128以内 否则不恢复任何已扣除的生命值上限（2026-09-22 拍板严格口径已实施：`DepthCurse` 取 max、仅 0 清零，见 spec A4；待实测）
- [ ] 待实测｜进度条折返瞬间归零、转头前进即恢复正常（实测修订：2026-09-22 锁存口径作废——`ZProgressHud` 去锁存记忆，回程段后半瞬时 0%、其余按公式，见 z256-bar §3.2/§4；待实测）
- [ ] 待实测｜虚空区域也有扣除生命值上限的机制 注意别冲突 或者把虚空的去掉（已按共存方案实现，各用各的 modifier，叠加效果待实测）
- [ ] 待实测｜每次跨越前才有提示 返回则没有 也不会恢复已被扣除的部份生命值 必须回到128以内才完全恢复（提示时机已由 z256 常驻条替代，滞后恢复逻辑待实测）
- [ ] 待实测｜提示条放在原本dimblend的位置 也改用进度条 最好根据接近程度更改颜色 数字显示为z绝对值（后半句“原本的dimblend进度条放到快捷栏左边的位置”已拍板忽略，不做）
- [ ] 作废｜玩家折返往回走时进度条锁0（旧“过0%回100%倒计”与 2026-09-22 回程锁存口径均已作废；现行瞬时归零口径见 `docs/z256-bar-requirement.md` §3.2）
- [ ] 待实测｜玩家位于z16以内时 隐藏进度条数字显示 仅保留进度条（2026-09-21 核对：代码已实现 `ZProgressHud.NUMBER_HIDE_RADIUS=16`，`absZ>16` 才绘制数字；文档滞后订正，待实测）
- [ ] 待实测｜阻止z64 z-64以内的所有生物生成（已收敛为仅拦自然生成，玩家/机器间接生成放行）
- [ ] 待实测｜信标提供的效果全图生效
- [ ] 待实测｜可以一键给背包里的物品或者面朝的砖块等起昵称 昵称会直接和物品id绑定 替代原本的名称显示 但完全不影响原本的任何属性（已收敛为 `/dbx nickname` 命令版：主手优先、空手取注视方块；2026-09-20 核对：命令+显示层代码在案，仅缺实跑；2026-09-22 用户回标未测试，状态不变）
- [ ] 待实测｜所有村民生成时或初次获得职业时 级别均为大师 但每个条目只能交易一次 且不会补货 职业方块丢失后也不会丢失职业（2026-09-22 口径已实施：`VillagerMasterRules` 收编+`VillagerMasterMixin` 拦补货/掉职业，见 spec G1；2026-09-24 新口径：rotating 内新生无业村民由 `FinalizeSpawnEvent` 打戳、扫描随机指派职业后收编，存量不追溯，傻子/婴儿/流浪商人跳过；待实测）
- [ ] 待实测｜只有位于 sable 结构上的床才可交互（2026-09-22 口径已实施：`StructureBedGuard` 右键拦截，见 spec G2；待实测）
- [ ] 待实测｜仅限天然生成的水源有无限水特点 任何人为或机器放置的水都不具有无限水特点（2026-09-22 口径已实施：`CreateFluidSourceEvent` 成形门+setBlock 记录位，见 spec G3；待实测）
- [ ] 待实测｜禁止所有形式的纬度传送门生成 禁止折跃门传送功能（2026-09-22 口径已实施：`PortalBan` 生成禁+离境禁+`WarpGateBlockMixin` 掐折跃门，见 spec G4；待实测）

## create:diesel_generator

> CLEAR：以下条目已实测通过，归档保留。

- [x] 已完成｜柴油发动机输入燃油后 转速会先达到16rpm 然后每4秒+2rpm 直到达到额定转速
- [x] 已完成｜柴油发动机燃油烧光后会正常停机
- [x] 已完成｜柴油机达到额定转速后 会在额定转速和80%转速之间随机跳变
- [x] 已完成｜柴油机运作时若应力过载后破坏自身（已由闩锁方案改为爆机掉落，余油不返还）
- [x] 已完成｜大型柴油引擎也要有热机时间和波动转速以及过载损坏（原“未实现”，开发侧已实现，待实测）

## create

- [ ] 待实测｜伪装方块硬度=黑曜石
- [ ] 待实测｜基于 伪装板 再添加一个变体“创造模式伪装板” 不可制作 仅能创造模式破坏和放置 且仅能用扳手移除材质不能用扳手拆卸（用于制作车架；2026-09-20 核对：代码已实现，见 spec C3）

## create:copycat

- [ ] 待实测｜伪装方块硬度=黑曜石
- [ ] 待实测｜基于 伪装半砖及其上半/下半/双砖变体 再添加一个变体“创造模式伪装半砖” 不可制作 仅能创造模式破坏和放置 且仅能用扳手移除材质不能用扳手拆卸（用于制作车架；2026-09-20 核对：代码已实现，见 spec C1）
- [ ] 待实测｜基于 伪装粱 添加一个变体“创造模式伪装粱” 不可制作 仅能创造模式破坏和放置 且仅能用扳手移除材质不不能用扳手拆卸（用于制作车勾；2026-09-20 核对：代码已实现，见 spec C2）

## create:aeronautics

- [ ] 待实测｜生存模式禁止摆放物理组装器：通过破坏自身阻止（2026-09-23 已实施「放行放置+tick 末拆除返还」：真人直回背包（无提示）、机器放置掉落，见 spec F1。取消方案实测物品消失不返还已作废——根因为客户端预测已扣物品而服务端物品净变化为零不再纠正；旧破坏自身的鬼影系事件回调内当场拆除、与放置事务的快照应用/回滚竞争所致，新实现延迟到 tick 末拆除；待实机验证）

## create:addon

- [ ] 待实测｜电动马达改为接收到红石信号才开始工作
- [ ] 待实测｜马达收到的红石信号从1-15作为其额定转速的转速比率 1为4rpm 15为额定转速 实时更新（原“仅检测输入时的强度 没有随动”已改为每服务端 tick 随动）
- [ ] 待实测｜电动马达低于4的额定转速所消耗能量与4相同
- [ ] 待实测｜电动马达耗电量基于实际转速而不是设定最高转速（2026-09-20 核对：代码已实现纯线性+4rpm钳定，见 spec D3）
- [ ] 待实测｜电动马达在设定转速高于64rpm时 会在收到任意强度的信号后播放运转声音（播放启动音效，三秒后持续播放运转音效）并在信号结束后播放停机音效（2026-09-23 用户回标"未实现"成立：原实现运转判据读 CCA `active` 字段，但 CCA 1.5.10 只在 writeSafe 写该键、不覆写 write，客户端同步包内永无 "active" → 客户端恒 false、三态机永不触发；已改判据为同步的 `speed`（"Speed" 标签=映射后实际转速），见 spec D4 v1.7；2026-09-23 用户追加：三处音效音量减半、可听范围约 16 格；2026-09-24 用户回标复查：loop/startup 素材为立体声，OpenAL 对立体声源不做距离衰减，16 格范围与线性衰减实际未生效——已降混为单声道（L/R 平均、峰值归一 -1dBFS、采样数不变保循环无缝）；2026-09-24 用户指定运转音素材换 `motor_running (1)`（1.32 秒），播放方式改为运转期间起单次音、忽略素材时长，代码不加淡入淡出、素材不裁剪，降混单声道以保 16 格衰减；实测反馈的淡入淡出为素材自带包络；2026-09-24 用户定：素材不变、间隔改每 5 tick、素材增益 +11dB（峰值 -3.8dBFS），待实机听音验证）
- [ ] 待实测｜交流发电机储存的电能FE 只在无有效转速输入（本 tick 不产电）时自行减少 约每秒减5000FE（2026-09-23 新条目已实施，2026-09-25 用户回标收敛：此前 |rpm|<16 口径有输入也净减少；现 `AlternatorIdleDrainMixin` 判据取产能门精确取反、有输入产电时不扣，每 tick 扣 250 FE、扣到 0 止，2026-09-25 用户改量 1000→5000/s，新开关 alternatorIdleDrain，见 spec D5；待实测）
- [ ] 待实测｜电动马达显示中的 应力量 和 已使用能量 改为实际转速下的响应数值（2026-09-23 新条目已实施：能耗行入参改 |speed|（D3 钳定同步生效、无信号显示 0）；应力量行原版因客户端 getGeneratedSpeed 恒 0 恒显示 0 su，现折算读数改 speed → MAX_STRESS/256×|实际映射转速|；两处挂 electricMotorBehavior，见 spec D6；待实测）

## create:simrail

- [ ] 待实测｜所有砖块内容 防爆防拆 仅限创造模式编辑
- [ ] 待实测｜取消车钩收到红石信号解锁的机制
- （已确认：列车以 sable 子层级为单位整体运作，音效按转向架粒度播放——见 requirements-spec §E0）
- [ ] 待实测｜增大列车行进声音音量（约目前基础的三倍；2026-09-23 用户条目行进声减半：`BogeyTrackSound.FULL_VOLUME` 3.0→1.5，见 spec E2）
- [ ] 待实测｜整列列车在有速度的时候按速度产生如下动态声音（整车单循环，见下条）
- [ ] 待实测｜所有列车车架会依照行进速度播放行进音效 标准10m/s 音阶1 最低2m/s 音阶0.5 最高16m/s 音阶1.5 淡入5秒、停播淡出1秒；素材为短促单次音、整车 leader 每秒起一条（间隔不随音调拉伸）（2026-09-22 拍板口径已实施：`BogeyTrackSound.LOW_PITCH=0.5`，见 spec E2；2026-09-23 根因修复：淡入起播零音量被 `SoundEngine` 跳过（"Skipped playing sound, volume was zero"），`canStartSilent` 已恒返 true，否则全车架无声；2026-09-24 实测失败收敛：30架各播一条30秒循环叠成混乱声，改整车单源（`TrainLoopLeader` 同子层级/25m车钩邻接连锁选leader、按集群最大速度驱动；车场内60m开外他车链不上，不抢播），停播淡出改为1秒+孤儿看门狗；2026-09-24 改 `motor_running` 短音、播放时机改为 1 秒一次；2026-09-24 用户回标：`motor_running` 是马达素材，换为列车素材 `train-running (2)`（1.14 秒短音），仍每秒一条、音调随速度，停播逻辑不变；待实测）
- [ ] 待实测｜刹车音效 音阶1 音量减半；仅速度大于 4 m/s 时播放（2026-09-22 拍板音阶 1.0 已实施；2026-09-23 用户条目音量减半已实施：`PhysicsBogeyBrakeSoundMixin.BRAKE_VOLUME=0.5`，松闸维持 1.0；v1.8 速度门 `TrainOffStructureMath.isBrakeAudible`，见 spec E3；待听音实测）
- [ ] 待实测｜解除刹车时会播放泄气阀音效（spec E4 口径维持原参数；2026-09-22 增量未单独提及，状态不变）
- [ ] 待实测｜刹车和解除刹车音效 所有车架都会播放（2026-09-22 用户回标未实现；2026-09-23 根因修复：① 服务端播音须先 `projectOutOfSubLevel` 投影到世界坐标（车勾同型），此前播局部坐标、声音落在 plot 区车旁听不见；② 触发从 `sable$physicsTick` 移到服务端 `tick()`，静置/休眠列车不断沿；2026-09-24 实测失败收敛：沿只能在受电单架上看到，改沿触发整车补播（`TrainBrakeBroadcast` 半径120m+速度窗1.5m/s，单次上限48，同速邻车误播为接受项），见 spec E3/E4；待实测）
- [ ] 待实测｜位于幻纱宽轨上面的车架 会在其 y-1 3x3 范围生成下落的末地烛粒子 密度和末地烛相近即可（2026-09-22 口径已实施：`PhysicsBogeyWideGaugeParticleMixin` 纯客户端，见 spec E6；2026-09-23 用户条目：仅 `railways:track_phantom_wide` 精确匹配、其他宽轨不播；待实测）
- [ ] 待实测｜任意车架速度大于 4 m/s 时每 10 秒以玩家为中心检测半径 32 格内是否有 sable 结构，都无则传送回重生位置（v1.8 已实施：`TrainOffStructureRules`，见 spec E7；待实测）
- [ ] 待实测｜车架速度大于 4 m/s 时 每固定 2 秒有（速度值/20×100%）的概率对车架施加 1200 pN 的横向力 方向垂直车架方向 随机左右 持续 10 tick（2026-09-24 新条目，用户初稿 5-20 秒后改 2-8 秒、力 12000 pN 后改 1200 pN，间隔后再改为固定 2 秒；拍板：逐车架独立计时/判定、作用点在车架位置、全维度、降速到 ≤4 撤销计时再超速重新开始 2 秒计时；v1.9 已实施：`PhysicsBogeyLateralForceMixin`，新开关 trainLateralForce，见 spec E8；待实测手感）
