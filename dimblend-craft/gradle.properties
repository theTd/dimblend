# 单仓冻结说明（userdev -> ModDevGradle 迁移前保留本文件形态，见独立迁移任务）：
# - org.gradle.* 子工程配置是死配置（Gradle 只读根与 GRADLE_USER_HOME），已删除，以根 gradle.properties 为准；
# - 下方 minecraft_version/_range、neo_version、loader_version_range 与根同名同值，
#   系有意影子（迁移前不动）；改基线时必须两边联动 bump，迁移时彻底删除共享键，
#   只留 neogradle.subsystems.parchment.* + mod_*。
# Gradle 9.x 本体须跑在 JVM 17+；本机默认 JAVA_HOME 指向 JDK 11 时，
# 用 $env:JAVA_HOME='C:\Program Files\Java\jdk-21' 再执行 gradlew（亦可在 IDE 里指定 Gradle JVM）。

#read more on this at https://github.com/neoforged/NeoGradle/blob/NG_7.0/README.md#apply-parchment-mappings
# you can also find the latest versions at: https://parchmentmc.org/docs/getting-started
neogradle.subsystems.parchment.minecraftVersion=1.21.1
neogradle.subsystems.parchment.mappingsVersion=2024.11.17
# Environment Properties
# You can find the latest versions here: https://projects.neoforged.net/neoforged/neoforge
# The Minecraft version must agree with the Neo version to get a valid artifact
minecraft_version=1.21.1
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[1.21.1]
# The Neo version must agree with the Minecraft version to get a valid artifact
neo_version=21.1.249
# The loader version range can only use the major version of FML as bounds
loader_version_range=[1,)

## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=dimblend_craft
# The human-readable display name for the mod.
mod_name=Dimblend Craft
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=All Rights Reserved
# The mod version. See https://semver.org/
mod_version=0.1.0
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=dimblend.craft
